# requirements.txt — dependencias del servidor MCP (opcional).
# SUGERENCIA (no obligación): el MCP es opcional para proyectos que quieran
# exponer definiciones a agentes. Python 3.12+ con uv. Estructura sugerida:
#   mcp/source/ (server.py, config.py, requirements.txt) + mcp/.venv/
mcp>=1.0
PyYAML>=6.0
