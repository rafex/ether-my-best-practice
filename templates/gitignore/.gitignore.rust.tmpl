# .gitignore — backend Rust (Cargo)
# Template de referencia: templates/gitignore/.gitignore.rust.tmpl
# Para el agente de IA: copiar a source/backend/rust/<proyecto>/.

# Build
target/

# Backups
*.rs.bk

# Cargo.lock
# Descomentar SOLO si es una aplicación binaria (no una librería)
# Cargo.lock
# Para librerías, Cargo.lock SE versiona
