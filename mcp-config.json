{
  "mcpServers": {
    "ether-rules": {
      "command": "uv",
      "args": ["run", "--directory", "mcp", "python", "ether_mcp_my_best_practices/server.py"],
      "env": {
        "RULES_DIR": "rules",
        "TEMPLATES_DIR": "templates"
      }
    }
  }
}
