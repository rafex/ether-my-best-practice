{
  "mcpServers": {
    "ether-rules": {
      "command": "uv",
      "args": ["run", "--directory", "helpers/mcp", "python", "source/server.py"],
      "env": {
        "RULES_DIR": "rules",
        "TEMPLATES_DIR": "templates"
      }
    }
  }
}
